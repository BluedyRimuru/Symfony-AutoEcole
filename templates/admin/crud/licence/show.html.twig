{% extends 'base.html.twig' %}

{% block title %}Licence{% endblock %}

{% block body %}
    <h1>Licence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ licence.id }}</td>
            </tr>
            <tr>
                <th>Dateobtention</th>
                <td>{{ licence.dateobtention ? licence.dateobtention|date('d-m-Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_licence_index') }}">back to list</a>

    <a href="{{ path('app_licence_edit', {'id': licence.id}) }}">edit</a>

    {{ include('crud/licence/_delete_form.html.twig') }}
{% endblock %}
